[Unit]
Description=每小时整点发送平台访问链接(含当前小时 token)
Documentation=file:/opt/stock/README.md

[Timer]
# 每小时的第 0 分钟(服务器时区 Asia/Shanghai)
OnCalendar=*-*-* *:00:00
# 若到点时服务器离线, 开机后补发一次(用当时小时重新生成 token)
Persistent=true
Unit=stock-token-mail.service

[Install]
WantedBy=timers.target
