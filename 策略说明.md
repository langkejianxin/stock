# 策略说明（ETF 监控平台）

> 两个策略的信号、执行时点、数据源与回测口径的完整说明（与 `strategy_lib.py` 实现一致）。

---

## 策略1：RSI+动量 共识（分歧各半）

> 设计文档见 **[共识策略文档.md](共识策略文档.md)**。

| 项 | 说明 |
|---|---|
| 标的 | 512800 银行ETF vs 159949 创业板50ETF，**永不空仓** |
| 数据源 | **场内前复权价格**（腾讯行情，`fetch_stock.py` 维护 `data/512800_股票.csv`、`data/159949_股票.csv`） |
| 信号 | 两个独立信号，每周五收盘比较：**RSI(14)**（日收益率 SMA 版）与 **动量(20)**（前复权收盘 20 日变化率） |
| 持仓 | 两信号**一致** → 满仓该标的；两信号**分歧** → **各持一半(50/50)**；分歧持续到重新一致 |
| 执行 | 次周一生效；采用 **周五收盘执行** 口径（周五收盘定权，吃满整周收益含周一跳空） |
| 回测(2017-08-03 ~ 2026-08-31) | 累计 **+464.16%** · 年化 21.00% · 最大回撤 -29.82% · 夏普 0.86 · 形态切换 170 次 |

**设计初衷**（共识策略文档）：信号一致时跟最强信号走，信号打架时被动分散，避免在冲突期押注单一方向——收益不输单信号、回撤显著改善、夏普更高（净值口径回测夏普 0.90 为三者最高）。

**执行价口径说明**（`compare_exec_price.py` 实测，原动量口径）：周五收盘执行(+427.24%) 优于周一开盘执行(+388.52%)——换仓周强势标的隔夜跳空延续（动量效应隔夜外推），因此主策略采用周五收盘执行口径。

---

## 策略2：滚500·95%分位通道

| 项 | 说明 |
|---|---|
| 标的 | 510880 红利ETF（华泰柏瑞上证红利） |
| 数据源 | **基金净值**（东方财富 F10，`fetch_nav.py` 维护 `data/510880_基金净值.csv`） |
| 通道 | 滚动 500 日线性回归（只用当天之前的数据拟合，**无未来函数**）；上轨 = 中轨 + 残差 97.5% 分位，下轨 = 中轨 + 残差 2.5% 分位 |
| 信号 | **每个交易日收盘判定**：空仓且累计净值 ≤ 下轨 → 买入；持仓且累计净值 ≥ 上轨 → 卖出；其余不动 |
| 执行 | **当天收盘判定 → 次日执行**（次日开盘起持仓，吃次日全天涨跌含跳空） |
| 回测(2019-01-02 ~ 2026-08-28) | 累计 **+237.22%** · 年化 17.22% · 最大回撤 -12.33% · 夏普 1.38 · 平均仓位 50.3% · 买卖 13 次 |

---

## 执行时点汇总

| 策略 | 信号时点 | 执行时点 |
|---|---|---|
| 策略1 RSI+动量 共识 | 每周五收盘（RSI 与 动量 分别比较，一致满仓/分歧各半） | 次周一生效，按**周五收盘价**买入 |
| 策略2 通道 | **每个交易日收盘**（净值 vs 上下轨） | **次日**生效（次日开盘持仓） |

两个策略均**不在信号当天盘中交易**：都是收盘定信号、下一交易日执行，只是节奏不同（策略1 每周一，策略2 每天判定、触轨才动）。

---

## 数据与文件

| 文件 | 作用 |
|---|---|
| `fetch_nav.py` | 抓基金净值（策略2 用）→ `data/*_基金净值.csv` |
| `fetch_stock.py` | 抓场内前复权行情（策略1 用）→ `data/{代码}_股票.csv` |
| `strategy_lib.py` | 两个策略的实时计算（仪表盘数据源） |
| `daily_signal.py` | 输出当日交易信号 → `output/今日信号.txt` |
| `compare_exec_price.py` | 周五收盘 vs 周一开盘执行价对比（研究用） |
| `run_daily.sh` | 每日 20:30：抓净值 + 抓行情 → 出信号 |

---

*仅供策略研究，不构成投资建议。*
